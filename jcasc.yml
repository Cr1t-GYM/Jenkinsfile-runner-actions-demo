credentials:
  system:
    domainCredentials:
      - credentials:
          - aws:
              accessKey: "AKIA5IBDKF6WM4UVSYEM"
              description: "aws access key for yiminggo"
              id: "aws_key"
              scope: GLOBAL
              secretKey: "${AWS_KEY}"
tool:
  maven:
    installations:
      - name: "maven"
        properties:
        - installSource:
            installers:
              - zip:
                  subdir: "apache-maven-3.8.6"
                  url: "https://home.markwaite.net/~mwaite/maven/apache-maven-3.8.6-bin.zip"
  mavenGlobalConfig:
    globalSettingsProvider: "standard"
    settingsProvider: "standard"
unclassified:
  globalItemStorage:
    storage:
      s3:
        bucketName: "jenkins-action"
        credentialsId: "aws_key"
        region: "us-east-2"