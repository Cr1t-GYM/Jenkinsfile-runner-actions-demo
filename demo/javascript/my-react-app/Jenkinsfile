pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                dir("${GITHUB_WORKSPACE}/demo/javascript/my-react-app") {
                    nodejs(nodeJSInstallationName: 'NodeJS 18.3.0') {
                        sh 'npm ci'
                    }
                }
            }
        }
    }
}